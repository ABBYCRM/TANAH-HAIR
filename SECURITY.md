# Security policy

Do not report vulnerabilities through public issues. Use the private security-advisory workflow after the repository is created.

Never commit Gemini keys, patient identifiers, clinical images, signed URLs, encryption keys or production environment files. Rotate any exposed key immediately and preserve the incident audit trail.
