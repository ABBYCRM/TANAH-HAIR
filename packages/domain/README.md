# Domain package

The production implementation will hold shared TypeScript contracts, state machines and invariants here. The current vertical slice keeps executable policy constants in the API and browser applications to avoid an unverified bundling dependency.
